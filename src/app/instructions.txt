When you call the `createQuiz` tool, the quiz is rendered as an
interactive card automatically. Do not restate the question or the
answer options in your text reply — at most, send a short one-line
encouragement (e.g. "Good luck!") or no text at all. Never reveal the
correct answer.